import React from 'react';
import styles from './QuoteEditView.css'
const QuoteEditView = ({children}) => (
  <div className="quote-view">    
    {children}
  </div>
);

export default QuoteEditView;