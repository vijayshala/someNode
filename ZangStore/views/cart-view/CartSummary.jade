block styles
  link(href='/public_nocache/stylesheets/cart/cart-checkout.css', rel='stylesheet')

.order-summary    
  .container
    hr
    if summaryTitle
      h2.cart-order-summary--ribbon #{summaryTitle}
    //- else
      if isQuote
        .main-title #{cartItem.product.title + ' Quote'}
      else
        .main-title #{isCheckout ? localizer.get('ORDER_SUMMARY') : localizer.get(cartItem.title.resource) + ' ' + localizer.get('SUMMARY')}
    .row
      .col-lg-12.col-md-12              
        include ./CartSummaryItemsDetails 
    
